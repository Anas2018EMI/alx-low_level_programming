#include "main.h"

/* betty style doc for function _atoi goes there */
/**
 * _atoi - Entry point
 * @s: First operand
 * Return: int
 */
int _atoi(char *s)
{
	if (*s)
	{
		return (1);
	}
	return (0);
}
